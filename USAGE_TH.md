# คู่มือการใช้งาน scrcpy-custom (ฉบับสมบูรณ์)

## สารบัญ
1. [การติดตั้ง](#การติดตั้ง)
2. [การตั้งค่าเบื้องต้น](#การตั้งค่าเบื้องต้น)
3. [การเชื่อมต่อ](#การเชื่อมต่อ)
4. [**การใช้ Android เป็น Hotspot** 🔥](HOTSPOT_MODE_TH.md) - **แนะนำ!**
5. [การปรับแต่งประสิทธิภาพ](#การปรับแต่งประสิทธิภาพ)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
7. [ตัวเลือกขั้นสูง](#ตัวเลือกขั้นสูง)

---

## การติดตั้ง

### ข้อกำหนดของระบบ

**Windows 10/11**:
- Rust 1.70 หรือใหม่กว่า
- FFmpeg พร้อม hardware acceleration
- GPU ที่รองรับ Vulkan/DX12 (สำหรับ wgpu)

**GPU ที่แนะนำ**:
- NVIDIA GTX 900 series ขึ้นไป (สำหรับ NVDEC)
- Intel Gen 7 ขึ้นไป (สำหรับ QSV)
- AMD GCN ขึ้นไป (สำหรับ VAAPI)

### ขั้นตอนที่ 1: ติดตั้ง Rust

```powershell
# ดาวน์โหลดและติดตั้งจาก
# https://rustup.rs/

# ตรวจสอบการติดตั้ง
rustc --version
cargo --version
```

### ขั้นตอนที่ 2: ติดตั้ง FFmpeg

**วิธีที่ 1: ผ่าน Chocolatey (แนะนำ)**
```powershell
# ติดตั้ง Chocolatey ก่อน (ถ้ายังไม่มี)
# จาก https://chocolatey.org/install

# ติดตั้ง FFmpeg
choco install ffmpeg-full
```

**วิธีที่ 2: ดาวน์โหลดโดยตรง**
1. ดาวน์โหลดจาก https://github.com/BtbN/FFmpeg-Builds/releases
2. เลือก `ffmpeg-n5.1-latest-win64-gpl-shared-5.1.zip`
3. แตกไฟล์และเพิ่ม `bin/` เข้า PATH

**ตรวจสอบการติดตั้ง**:
```powershell
ffmpeg -version
# ควรเห็น ffmpeg version 5.x หรือใหม่กว่า
```

### ขั้นตอนที่ 3: คอมไพล์โปรเจค

```powershell
cd C:\Users\beok1\Desktop\scrcpy-custom
cargo build --release
```

**หมายเหตุ**: การคอมไพล์ครั้งแรกอาจใช้เวลา 5-10 นาที

---

## การตั้งค่าเบื้องต้น

### การเตรียม Android Device

1. **เปิด USB Debugging**:
   - ไปที่ Settings → About phone
   - แตะ "Build number" 7 ครั้งเพื่อเปิด Developer mode
   - กลับไปที่ Settings → Developer options
   - เปิด "USB debugging"

2. **ติดตั้ง ADB** (Android Debug Bridge):
```powershell
choco install adb
```

3. **ตรวจสอบการเชื่อมต่อ**:
```powershell
adb devices
# ควรเห็นอุปกรณ์ของคุณ
```

---

## การเชื่อมต่อ

### วิธีที่ 1: การเชื่อมต่อผ่าน USB (แนะนำสำหรับ Latency ต่ำ)

**ความหน่วงที่คาดหวัง**: < 50ms

```powershell
# 1. เชื่อมต่อ Android ผ่าน USB
# 2. Forward port ผ่าน ADB
adb forward tcp:5555 tcp:5555

# 3. รันโปรแกรม
cd C:\Users\beok1\Desktop\scrcpy-custom
cargo run --release -- --mode tcp --host 127.0.0.1 --port 5555
```

**ตัวอย่างผลลัพธ์**:
```
[INFO] Starting scrcpy-custom
[INFO] Using TCP connection (wired/USB)
[INFO] Connecting to 127.0.0.1:5555...
[INFO] Connected successfully!
[INFO] Using hardware decoder: h264_cuvid
[INFO] Using GPU: NVIDIA GeForce RTX 3060
[INFO] Starting receive loop...
```

### วิธีที่ 2: การเชื่อมต่อผ่าน WiFi (สำหรับความยืดหยุ่น)

**ความหน่วงที่คาดหวัง**: < 100ms (ขึ้นกับคุณภาพ WiFi)

```powershell
# 1. หา IP ของ Android
adb shell ip addr show wlan0 | findstr "inet "
# หรือดูใน Settings → About phone → Status → IP address

# 2. รันโปรแกรม (สมมติ IP = 192.168.1.100)
cargo run --release -- --mode quic --host 192.168.1.100 --port 5556
```

---

## การปรับแต่งประสิทธิภาพ

### การเลือก Hardware Decoder

**NVIDIA GPU (NVDEC)**:
```powershell
cargo run --release -- --hw-decoder nvdec --mode tcp
```

**Intel GPU (Quick Sync Video)**:
```powershell
cargo run --release -- --hw-decoder qsv --mode tcp
```

**AMD GPU (VAAPI - Linux only)**:
```bash
cargo run --release -- --hw-decoder vaapi --mode tcp
```

**Auto-detect (แนะนำ)**:
```powershell
cargo run --release -- --hw-decoder auto --mode tcp
```

### การปรับ Bitrate

**สำหรับ USB (คุณภาพสูง)**:
```powershell
cargo run --release -- --bitrate 12 --mode tcp
```

**สำหรับ WiFi (เสถียรภาพ)**:
```powershell
cargo run --release -- --bitrate 6 --mode quic
```

**อัตราส่วน Bitrate ที่แนะนำ**:
- 2-4 Mbps: WiFi อ่อน, ความละเอียด 720p
- 6-8 Mbps: WiFi ปานกลาง, ความละเอียด 1080p
- 10-12 Mbps: USB, ความละเอียดสูง 1080p
- 15-20 Mbps: USB, คุณภาพสูงสุด

### Adaptive Bitrate (ปรับอัตโนมัติ)

เปิดใช้งานอัตโนมัติ (default):
```powershell
cargo run --release -- --adaptive-bitrate --mode quic
```

ปิดใช้งาน (fixed bitrate):
```powershell
cargo run --release -- --adaptive-bitrate false --bitrate 8 --mode tcp
```

---

## การแก้ไขปัญหา

### ปัญหา 1: "No video decoder available"

**สาเหตุ**: FFmpeg ไม่ได้ติดตั้งหรือไม่พบ

**วิธีแก้**:
```powershell
# ตรวจสอบ FFmpeg
ffmpeg -version

# ถ้าไม่พบ ติดตั้งใหม่
choco install ffmpeg-full

# ตรวจสอบ PATH
echo $env:PATH | Select-String ffmpeg
```

### ปัญหา 2: "Connection timeout"

**สาเหตุ**: Android server ไม่ได้รัน หรือ firewall บล็อก

**วิธีแก้**:
```powershell
# ตรวจสอบการเชื่อมต่อ
adb devices

# ตรวจสอบ port forwarding
adb forward --list

# ลบและสร้างใหม่
adb forward --remove tcp:5555
adb forward tcp:5555 tcp:5555

# ตรวจสอบ firewall Windows
# Control Panel → Windows Defender Firewall → Allow an app
```

### ปัญหา 3: "High latency" (ความหน่วงสูง)

**วิธีแก้**:

1. **ใช้ USB แทน WiFi**:
```powershell
cargo run --release -- --mode tcp  # แทน --mode quic
```

2. **ลด bitrate**:
```powershell
cargo run --release -- --bitrate 4
```

3. **ตรวจสอบ hardware acceleration**:
```powershell
cargo run --release -- --hw-decoder auto
# ดูใน log ว่าใช้ hardware decoder หรือไม่
```

4. **ปิดโปรแกรมอื่นที่ใช้ GPU**:
   - ปิด Chrome (hardware acceleration)
   - ปิดเกมส์
   - ปิด video editing software

### ปัญหา 4: "Audio glitches" (เสียงกระตุก)

**สำหรับ WiFi**:
```powershell
# Jitter buffer จะปรับอัตโนมัติ
# ถ้ายังมีปัญหา ให้ลด bitrate
cargo run --release -- --mode quic --bitrate 4
```

**สำหรับ USB**:
```powershell
# ตรวจสอบให้แน่ใจว่าใช้ USB 3.0
# ลองปลั๊กใน port อื่น
```

### ปัญหา 5: "Screen flickering" (หน้าจอกระพริบ)

**วิธีแก้**:
1. อัพเดท GPU driver
2. ลดความละเอียดหน้าจอ
3. ตรวจสอบ cable USB

---

## ตัวเลือกขั้นสูง

### คำสั่งแบบเต็ม

```powershell
cargo run --release -- `
    --mode quic `
    --host 192.168.1.100 `
    --port 5556 `
    --bitrate 8 `
    --hw-decoder nvdec `
    --hw-accel `
    --adaptive-bitrate
```

### ตัวอย่างการใช้งานแต่ละสถานการณ์

**1. เกมมิ่ง (Latency ต่ำสุด)**:
```powershell
cargo run --release -- `
    --mode tcp `
    --bitrate 12 `
    --hw-decoder nvdec `
    --adaptive-bitrate false
```

**2. ดูหนัง/วิดีโอ (คุณภาพสูง)**:
```powershell
cargo run --release -- `
    --mode tcp `
    --bitrate 15 `
    --hw-decoder auto
```

**3. ใช้งานทั่วไปผ่าน WiFi**:
```powershell
cargo run --release -- `
    --mode quic `
    --bitrate 6 `
    --adaptive-bitrate
```

**4. WiFi อ่อน (เน้นเสถียรภาพ)**:
```powershell
cargo run --release -- `
    --mode quic `
    --bitrate 3 `
    --adaptive-bitrate
```

### การวัดประสิทธิภาพ

โปรแกรมจะแสดง stats ทุก ๆ วินาที:
```
FPS: 60.0 | Latency: 45.2ms | RTT: 12.3ms | Loss: 0.05% | Drift: 2ms | Dropped: 0
```

**คำอธิบาย**:
- **FPS**: เฟรมต่อวินาที (ควรอยู่ที่ 60)
- **Latency**: ความหน่วง end-to-end (ควร < 50ms สำหรับ USB)
- **RTT**: Round-Trip Time (ควร < 20ms สำหรับ USB)
- **Loss**: Packet loss (ควร < 1% สำหรับ WiFi)
- **Drift**: A/V sync drift (ควร < 50ms)
- **Dropped**: จำนวน frame ที่ตก

### Environment Variables

```powershell
# เปิด debug logging
$env:RUST_LOG = "debug"
cargo run --release -- --mode tcp

# เฉพาะ networking debug
$env:RUST_LOG = "scrcpy_custom::network=debug"
cargo run --release -- --mode tcp
```

---

## Tips & Best Practices

### 1. การเพิ่มประสิทธิภาพ

- ✅ ใช้ USB 3.0 port
- ✅ ปิด WiFi บน Android เมื่อใช้ USB
- ✅ ใช้ WiFi 5GHz แทน 2.4GHz
- ✅ เชื่อม Android และ PC เข้า router เดียวกัน
- ✅ อัพเดท GPU driver เป็นเวอร์ชันล่าสุด

### 2. การประหยัด Battery

```powershell
# ลด bitrate และ FPS
cargo run --release -- --bitrate 4
```

### 3. การใช้งานกับหลายหน้าจอ

โปรแกรมจะ render บนหน้าจอที่มี focus  
สามารถเลื่อน window ไปหน้าจออื่นได้

### 4. Keyboard Shortcuts (อนาคต)

- `F11`: Fullscreen
- `Ctrl+Q`: Quit
- `Ctrl+S`: Screenshot
- `F1`: Toggle stats overlay

---

## การอัพเดท

```powershell
# Pull latest version
git pull origin main

# Rebuild
cargo build --release
```

---

## การขอความช่วยเหลือ

หากพบปัญหา:

1. ตรวจสอบ log ใน console
2. เปิด debug logging: `$env:RUST_LOG = "debug"`
3. ตรวจสอบ `implementation_summary.md` สำหรับรายละเอียดทางเทคนิค

---

## สรุปคำสั่งที่ใช้บ่อย

```powershell
# USB - คุณภาพสูง
adb forward tcp:5555 tcp:5555
cargo run --release -- --mode tcp --bitrate 12

# WiFi - สมดุล
cargo run --release -- --mode quic --host <IP> --bitrate 6

# Debug mode
$env:RUST_LOG = "debug"
cargo run --release -- --mode tcp

# ดู help
cargo run --release -- --help
```

---

**เวอร์ชัน**: 0.1.0  
**อัพเดทล่าสุด**: 2025-12-07
