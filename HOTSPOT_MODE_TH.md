# การใช้งานแบบ Android เป็น Hotspot Host

## ภาพรวม

วิธีนี้เหมาะสำหรับ:
- ไม่มี WiFi Router
- ต้องการเชื่อมต่อโดยตรงระหว่าง Android กับ PC
- ใช้งานในที่ที่ไม่มี WiFi
- ต้องการความเร็วและความหน่วงต่ำสุด (เพราะไม่ผ่าน Router)

---

## วิธีการ 1: Android เป็น Hotspot (แนะนำ)

### ขั้นตอนที่ 1: เปิด Hotspot บน Android

```
1. เข้า Settings → Network & Internet → Hotspot & tethering
2. เปิด "WiFi hotspot"
3. กด "Set up WiFi hotspot" เพื่อกำหนดค่า:
   - Network name: "AndroidMirror" (หรือชื่ือที่ต้องการ)
   - Security: WPA2-PSK
   - Password: ตั้งรหัสผ่าน
   - AP Band: 5GHz (หากรองรับ) สำหรับความเร็วสูง
4. เปิด Hotspot
```

**หมายเหตุ**: การใช้ 5GHz จะให้ความเร็วสูงกว่าและความหน่วงต่ำกว่า 2.4GHz มาก

### ขั้นตอนที่ 2: เชื่อมต่อ PC กับ Hotspot

**Windows**:
```
1. คลิก WiFi icon ที่ system tray
2. เลือก "AndroidMirror" (หรือชื่อที่ตั้งไว้)
3. ใส่รหัสผ่าน
4. เชื่อมต่อ
```

**ตรวจสอบการเชื่อมต่อ**:
```powershell
# ดู IP address ของ PC
ipconfig | findstr "192.168"

# Ping ไป Android hotspot (มักจะเป็น 192.168.43.1)
ping 192.168.43.1
```

### ขั้นตอนที่ 3: หา IP ของ Android

**วิธีที่ 1: ผ่าน ADB** (ต้องเชื่อมต่อ USB ก่อน):
```powershell
# เชื่อม USB ก่อน
adb shell ip addr show wlan0 | findstr "inet "

# ตัวอย่างผลลัพธ์:
# inet 192.168.43.1/24 brd 192.168.43.255 scope global wlan0
```

IP ของ Android hotspot มักจะเป็น: **192.168.43.1**

**วิธีที่ 2: ดูใน Settings** (Android 11+):
```
Settings → About phone → Status → IP address
```

**วิธีที่ 3: ใช้ Network Scanner App** (ถ้าไม่มี ADB):
- Install "Fing" หรือ "Network Scanner" จาก Play Store
- Scan network
- ดู IP ของตัวเอง

### ขั้นตอนที่ 4: รัน scrcpy-custom

```powershell
cd C:\Users\beok1\Desktop\scrcpy-custom

# ใช้ QUIC mode สำหรับ WiFi Direct
cargo run --release -- `
    --mode quic `
    --host 192.168.43.1 `
    --port 5556 `
    --bitrate 8 `
    --adaptive-bitrate
```

---

## วิธีการ 2: PC เป็น Hotspot, Android เชื่อมต่อเข้ามา

### ขั้นตอนที่ 1: เปิด Mobile Hotspot บน Windows

```
1. Settings → Network & Internet → Mobile hotspot
2. เลือก "Share my Internet connection from": WiFi หรือ Ethernet
3. Network band: 5GHz (หากรองรับ)
4. กำหนด:
   - Network name: "PCMirror"
   - Network password: ตั้งรหัสผ่าน
5. เปิด Mobile hotspot
```

### ขั้นตอนที่ 2: เชื่อมต่อ Android กับ PC Hotspot

```
1. บน Android เข้า Settings → WiFi
2. เลือก "PCMirror"
3. ใส่รหัสผ่าน
4. เชื่อมต่อ
```

### ขั้นตอนที่ 3: หา IP ของ Android

**ผ่าน ADB**:
```powershell
adb shell ip addr show wlan0 | findstr "inet "
```

**หรือดูใน Windows**:
```powershell
# ดูอุปกรณ์ที่เชื่อมต่อกับ hotspot
netsh wlan show hostednetwork

# หรือใช้ Network Scanner
```

### ขั้นตอนที่ 4: รัน scrcpy-custom

```powershell
cargo run --release -- `
    --mode quic `
    --host 192.168.137.xxx `  # แทนด้วย IP ที่ได้
    --port 5556
```

---

## เปรียบเทียบวิธีการ

| วิธีการ | ข้อดี | ข้อเสีย | แนะนำ |
|---------|-------|---------|-------|
| **Android เป็น Hotspot** | - ใช้งานง่าย<br>- ไม่ต้องตั้งค่า PC<br>- รองรับ 5GHz ส่วนใหญ่ | - แบตเตอรี่หมดเร็ว<br>- ร้อนได้ | ⭐⭐⭐⭐⭐ |
| **PC เป็น Hotspot** | - Android ประหยัดแบต<br>- PC มีไฟเสียบตลอด | - ต้องตั้งค่า PC<br>- บาง WiFi card ไม่รองรับ | ⭐⭐⭐ |

---

## การตั้งค่าเพิ่มเติมสำหรับ Hotspot Mode

### 1. เพิ่มประสิทธิภาพ WiFi

**บน Android**:
```
Settings → Developer options:
- Keep WiFi on during sleep: Always
- WiFi scan throttling: ปิด
```

**บน Windows**:
```powershell
# ตั้งค่า WiFi adapter ให้ใช้ Performance สูงสุด
Control Panel → Power Options → Change plan settings
→ Change advanced power settings
→ Wireless Adapter Settings → Power Saving Mode → Maximum Performance
```

### 2: ปรับ Bitrate ให้เหมาะสม

เนื่องจาก WiFi Direct มักจะมีสัญญาณแรง:

```powershell
# เริ่มต้นด้วย bitrate สูง
cargo run --release -- `
    --mode quic `
    --host 192.168.43.1 `
    --bitrate 12 `  # สูงกว่าปกติ
    --adaptive-bitrate
```

### 3. ตรวจสอบคุณภาพสัญญาณ

```powershell
# ตรวจสอบ signal strength
netsh wlan show interfaces

# ควรเห็น:
# Signal: 100% (ถ้าอยู่ใกล้กัน)
# Radio type: 802.11ac (5GHz) หรือ 802.11n
```

---

## การแก้ไขปัญหา Hotspot Mode

### ปัญหา 1: เชื่อมต่อไม่ได้

**สาเหตุ**: Firewall บล็อก

**วิธีแก้**:
```powershell
# เปิด port บน Windows Firewall
netsh advfirewall firewall add rule `
    name="scrcpy-custom QUIC" `
    dir=in `
    action=allow `
    protocol=UDP `
    localport=5556

# หรือปิด Firewall ชั่วคราว
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False
```

### ปัญหา 2: ความหน่วงสูง

**สาเหตุ**: ใช้ 2.4GHz band

**วิธีแก้**:
```
1. เปลี่ยนเป็น 5GHz ใน hotspot settings
2. ตรวจสอบว่า PC WiFi card รองรับ 5GHz
3. ลด bitrate เป็น 6-8 Mbps
```

### ปัญหา 3: Connection timeout

**สาเหตุ**: IP address ผิด

**วิธีแก้**:
```powershell
# หา IP ที่ถูกต้อง
adb shell ip addr show wlan0

# หรือ ping ลอง
ping 192.168.43.1
ping 192.168.137.1
```

### ปัญหา 4: Android ร้อนมาก

**วิธีแก้**:
```
1. ลด bitrate ลง:
   cargo run --release -- --bitrate 6

2. ใช้พัดลมเป่า หรือวางบนแผ่นระบายความร้อน

3. ไม่ заряด ขณะใช้งาน (ถ้าแบตพอ)

4. ลดความสว่างหน้าจอ Android
```

---

## Tips สำหรับ Hotspot Mode

### 💡 เพิ่มประสิทธิภาพสูงสุด

1. **วางใกล้กัน**: ระยะห่าง < 2 เมตร
2. **ไม่มีสิ่งกีดขวาง**: ไม่มีกำแพงหรือโลหะระหว่างอุปกรณ์
3. **ใช้ 5GHz**: เสมอหากรองรับ
4. **ปิดแอพอื่น**: บน Android ปิดแอพพื้นหลัง

### ⚡ ความเร็วที่คาดหวัง

| Band | ระยะ | Expected Speed | Latency |
|------|------|----------------|---------|
| 5GHz | < 2m | 100-300 Mbps | 20-40ms |
| 5GHz | 2-5m | 50-150 Mbps | 40-60ms |
| 2.4GHz | < 5m | 20-50 Mbps | 60-100ms |

### 🔋 การจัดการพลังงาน

**ถ้าใช้นาน > 30 นาที**:
```
1. เสียบสายชาร์จ Android
2. ใช้ USB Power Delivery (PD) หากรองรับ
3. เปิดโหมดประหยัดพลังงาน บน Windows
```

---

## ตัวอย่างคำสั่งที่ใช้บ่อย

### สถานการณ์ 1: ใช้งานทั่วไป

```powershell
# Android hotspot (IP มักเป็น 192.168.43.1)
cargo run --release -- `
    --mode quic `
    --host 192.168.43.1 `
    --port 5556 `
    --bitrate 8
```

### สถานการณ์ 2: เกมมิ่ง (ต้องการ latency ต่ำ)

```powershell
# ใช้ bitrate ปานกลาง, adaptive ปิด
cargo run --release -- `
    --mode quic `
    --host 192.168.43.1 `
    --port 5556 `
    --bitrate 6 `
    --adaptive-bitrate false
```

### สถานการณ์ 3: ดูหนัง (ต้องการคุณภาพสูง)

```powershell
# Bitrate สูง
cargo run --release -- `
    --mode quic `
    --host 192.168.43.1 `
    --port 5556 `
    --bitrate 12 `
    --adaptive-bitrate
```

---

## สรุป

**ข้อดี Hotspot Mode**:
- ✅ ไม่ต้องมี Router
- ✅ Latency ต่ำ (เชื่อมต่อตรง)
- ✅ ตั้งค่าง่าย
- ✅ ใช้งานได้ทุกที่

**ข้อควรระวัง**:
- ⚠️ แบตเตอรี่หมดเร็ว (ควรเสียบปลั๊ก)
- ⚠️ อุปกรณ์ร้อน (ใช้การระบายความร้อน)
- ⚠️ ต้องอยู่ใกล้กัน (< 5 เมตร)

**แนะนำ**: ใช้ 5GHz band + bitrate 8-10 Mbps + ระยะ < 2 เมตร = ประสบการณ์ที่ดีที่สุด! 🚀
